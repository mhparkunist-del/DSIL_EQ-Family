/* =========================================================================
 * API 어댑터 — config 에 따라 live / demo 를 자동 선택.
 * 페이지 스크립트는 항상 window.api 만 사용합니다.
 * ========================================================================= */
window.api = window.DSIL_CONFIG.API_URL ? window.apiLive : window.apiDemo;
